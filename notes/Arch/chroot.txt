mkdir /mnt/arch
mount /dev/sda? /mnt/arch
cd /mnt/arch/
mount -t proc proc proc
mount -t sysfs sys sys
mount -o bind /dev/ dev/
mount /dev/sda? boot/
ls -lh boot/
chroot .bin/bash
umount proc sys dev boot
cd ..
umount arch
